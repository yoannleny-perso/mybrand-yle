---
import { getCollection } from 'astro:content';
import ConceptsIndex from '../../../components/pages/ConceptsIndex.astro';
const entries = await getCollection('concepts');
---
<ConceptsIndex locale="es" entries={entries} />
