---
import { getCollection } from 'astro:content';
import InsightsIndex from '../../components/pages/InsightsIndex.astro';
const entries = await getCollection('insights');
---
<InsightsIndex locale="en" entries={entries} />
